# Cargo build output goes to the workspace root target/ directory now, which is
# ignored there. This entry stays only to catch a stray src-tauri/target/ left
# behind by a pre-workspace build.
/target/

# Generated by Tauri
# will have schema files for capabilities auto-completion
/gen/schemas
